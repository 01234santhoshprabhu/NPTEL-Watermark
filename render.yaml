services:
  - type: web
    name: nptel-watermark
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run image_watermark_app.py --server.port $PORT --server.address 0.0.0.0 --server.headless true
    autoDeploy: true
